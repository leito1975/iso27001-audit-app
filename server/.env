# Server Configuration
PORT=3001
NODE_ENV=development

# Database — set DATABASE_URL as env var in Render (production)
# or in .env.local for local development
DATABASE_URL=

# JWT
JWT_SECRET=dev-secret-key-iso27001-audit-app-2024
JWT_EXPIRES_IN=7d

# Frontend URL (for CORS)
FRONTEND_URL=http://localhost:5173
